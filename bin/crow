#!/usr/bin/env -S deno run --allow-read --allow-env --allow-net
/**
 * crow — CLI entry point. Delegates to src/cli.ts (compiled → dist/cli.js).
 */
import { run } from '../dist/cli.js'

run(Deno.args[0]).catch((error) => {
  console.error(error)
  Deno.exit(1)
})
