#!/usr/bin/env bash
# Run the test suite with coverage. Any arguments are forwarded to deno test.
set -euo pipefail

exec deno test --coverage --allow-env --allow-read --allow-write --allow-run --allow-sys=hostname "$@"
